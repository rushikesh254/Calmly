/**
 * PostCSS Configuration for Calmly Mental Health Platform
 * @author Rushikesh Bodke
 * @type {import('postcss-load-config').Config}
 */
const config = {
	plugins: {
		tailwindcss: {},
		autoprefixer: {},
	},
};

export default config;
